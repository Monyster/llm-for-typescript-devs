# Модуль 17: A2A, Computer Use та передові техніки

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Розуміти A2A протокол для комунікації між агентами
- Знати можливості Computer Use (керування браузером та десктопом)
- Використовувати мультимодальність (зображення, аудіо, відео)
- Розуміти тренд "Vibe Coding" та його обмеження

**Які задачі це дозволяє вирішувати:** Побудувати систему де агенти різних компаній спілкуються між собою (A2A). Автоматизувати роботу з legacy-системами через браузер. Обробляти зображення, PDF, аудіо та відео.

---

## 💰 Бізнес-цінність

**Проблема клієнта:** "Нам потрібні AI-агенти що співпрацюють між собою / AI що керує браузером."
**Рішення з цього модуля:** A2A (Agent-to-Agent), Computer Use — передній край AI-інженерії.
**Як продати:** Cutting-edge = premium rate. Спеціалізація на A2A/Computer Use = $200+/год. Мало хто це вміє.

## 17.1 A2A — Agent-to-Agent Protocol

### Що це

A2A — це стандарт від Google (квітень 2025, Linux Foundation з червня 2025) для комунікації між AI-агентами різних систем.

```
MCP: Агент ↔ Дані/Інструменти (агент використовує tools)
A2A: Агент ↔ Агент (агенти спілкуються між собою)
```

### Як працює

Кожен агент публікує **Agent Card** — JSON-файл що описує його можливості:

```json
{
  "name": "Travel Booking Agent",
  "description": "Бронює авіаквитки та готелі",
  "url": "https://travel.example.com/.well-known/agent.json",
  "skills": [
    {
      "id": "book_flight",
      "name": "Book Flight",
      "description": "Знайти та забронювати авіаквиток",
      "inputSchema": {
        "type": "object",
        "properties": {
          "from": { "type": "string" },
          "to": { "type": "string" },
          "date": { "type": "string", "format": "date" }
        }
      }
    }
  ],
  "authentication": { "schemes": ["oauth2"] }
}
```

### Сценарій: Бізнес-подорож

```
Ваш корпоративний агент:
  "Потрібна подорож Київ → Лондон, 15-18 березня, бюджет $3000"
        │
        ├── A2A → Travel Agent: "Знайди рейс KBP→LHR 15.03"
        │         → Повертає варіанти, ціни
        │
        ├── A2A → Hotel Agent: "Знайди готель в Лондоні 15-18.03"
        │         → Повертає варіанти
        │
        ├── A2A → Calendar Agent: "Перевір конфлікти 15-18.03"
        │         → Підтверджує що слот вільний
        │
        └── Агент компонує план → Human-in-the-loop → Бронювання
```

### Статус (2026)

A2A v0.3 (липень 2025), 150+ організацій підтримують. Поки переважно у enterprise-середовищі. Для більшості проектів MCP достатньо — A2A потрібен коли треба інтегрувати агентів різних компаній.

---

## 17.2 Computer Use — AI керує комп'ютером

### Що це

Computer Use — це можливість AI-агента взаємодіяти з комп'ютером як людина: клікати, друкувати, робити скріншоти, читати з екрану.

### Anthropic Computer Use

```typescript
import Anthropic from '@anthropic-ai/sdk';

const client = new Anthropic();

const response = await client.messages.create({
  model: 'claude-sonnet-4-5-20250929',
  max_tokens: 4096,
  tools: [
    {
      type: 'computer_20250124',  // Версія від січня 2025
      name: 'computer',
      display_width_px: 1920,
      display_height_px: 1080,
    },
  ],
  messages: [
    {
      role: 'user',
      content: 'Відкрий браузер, зайди на GitHub і створи новий репозиторій "my-project"',
    },
  ],
});

// Агент повертає дії:
// { type: 'click', coordinates: [500, 300] }
// { type: 'type', text: 'my-project' }
// { type: 'screenshot' }  — для перевірки результату
```

### Browser-Use (open-source, 50K+ GitHub stars)

```typescript
import { BrowserAgent } from 'browser-use';

const agent = new BrowserAgent({
  model: 'gpt-5',
  headless: true,
});

const result = await agent.run(
  'Зайди на amazon.com, знайди бездротові навушники до $50, додай найкращі в кошик'
);
```

### Коли використовувати Computer Use

| Сценарій | Чи підходить | Чому |
|----------|-------------|------|
| Legacy система без API | ✅ Ідеально | Єдиний спосіб автоматизації |
| Web scraping складних сайтів | ✅ Добре | Розуміє контекст, а не лише HTML |
| Тестування UI | ✅ Добре | Тестує як реальний користувач |
| Задачі з API | ❌ Не потрібно | API швидший, надійніший, дешевший |
| Batch обробка даних | ❌ Не підходить | Занадто повільно |

---

## 17.3 Мультимодальність на практиці

### Аналіз зображень

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { readFileSync } from 'fs';

// Аналіз скріншота помилки
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  messages: [{
    role: 'user',
    content: [
      { type: 'text', text: 'Що за помилка на скріншоті? Як її виправити?' },
      {
        type: 'image',
        image: readFileSync('./error-screenshot.png'),
      },
    ],
  }],
});

// "На скріншоті видно TypeError: Cannot read property 'map' of undefined.
//  Проблема: масив items не ініціалізований. Рішення: ..."
```

### Аналіз PDF

```typescript
import { generateText } from 'ai';
import { anthropic } from '@ai-sdk/anthropic';

// Claude найкраще працює з PDF
const { text } = await generateText({
  model: anthropic('claude-sonnet-4-5-20250929'),
  messages: [{
    role: 'user',
    content: [
      {
        type: 'document',
        source: {
          type: 'base64',
          mediaType: 'application/pdf',
          data: readFileSync('./contract.pdf').toString('base64'),
        },
      },
      { type: 'text', text: 'Витягни ключові умови контракту: сума, термін, штрафи.' },
    ],
  }],
});
```

### Генерація зображень

```typescript
import OpenAI from 'openai';

const client = new OpenAI();

const image = await client.images.generate({
  model: 'dall-e-3',
  prompt: 'Мінімалістичний логотип для TypeScript AI SDK бібліотеки, синьо-фіолетові тони',
  size: '1024x1024',
  quality: 'hd',
});

console.log(image.data[0].url);
```

---

## 17.4 Vibe Coding — кодування через AI

### Що це

Термін Andrej Karpathy (лютий 2025): замість написання коду — опис того, що потрібно, а AI генерує код. Collins Dictionary обрав "Vibe Coding" словом 2025 року.

### Реальність

- YC W25: 25% стартапів мають 95%+ AI-generated codebase
- Cursor: $500M ARR (червень 2025)

### Обмеження та ризики

- В 1.7 рази більше major issues порівняно з ручним кодом
- В 2.74 рази більше security vulnerabilities
- Проблема "almost right, but not quite" (66% розробників)

**Висновок для курсу:** Vibe coding — це потужний інструмент для прототипування, але для production-якості потрібно розуміти що відбувається під капотом. Саме тому цей курс навчає фундаменту, а не лише "як попросити AI написати код".

---

## Перевір себе

1. Чим A2A відрізняється від MCP? Коли що використовувати?
2. Що таке Agent Card в A2A?
3. Коли Computer Use краще ніж API? Коли гірше?
4. Напишіть код який аналізує скріншот і описує UI-проблеми
5. Які ризики vibe coding і як їх мінімізувати?

---

**Назад:** [← Модуль 16 — Evaluation](16-evaluation.md) | **Далі:** [Модуль 18 — Бізнес →](18-business.md)
