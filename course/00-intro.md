# Модуль 00: Вступ — Що таке LLM і навіщо це розробнику

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Розуміти що таке LLM і як воно працює (без математики)
- Знати чим відрізняються GPT-5, Claude, Gemini та коли яку модель обирати
- Розуміти чому TypeScript став мовою №1 для AI-розробки
- Мати налаштоване середовище для всіх наступних модулів

**Які задачі це дозволяє вирішувати:** Ви зможете усвідомлено обговорювати AI-проекти з командою, оцінювати чи підходить LLM для конкретної задачі, і зрозуміти межі того, що LLM може і не може.

---

## Що таке LLM простими словами

**LLM (Large Language Model)** — це програма, яка навчилась передбачати наступне слово на основі мільярдів текстів з інтернету. Але завдяки масштабу навчання, вона навчилась не просто "автодоповнювати", а розуміти контекст, слідувати інструкціям, писати код, аналізувати дані та вирішувати складні задачі.

### Аналогія для розробників

Уявіть, що LLM — це **надзвичайно досвідчений колега**, який:
- Прочитав всю документацію Stack Overflow, GitHub, MDN, та тисячі книг
- Має досвід у будь-якій технології, але іноді помиляється у деталях
- Працює миттєво, 24/7, і коштує копійки за запит
- Не пам'ятає попередні розмови (якщо ви спеціально не реалізуєте пам'ять)

### Що LLM вміє (і що ні)

**Добре працює:**
- Генерація та аналіз тексту (документація, email, контент)
- Написання, рев'ю та рефакторинг коду
- Витягування структурованих даних з хаотичного тексту
- Класифікація (тональність, категорії, інтент)
- Переклад між мовами
- Відповіді на питання по документах
- Планування та розбиття задач на кроки

**Працює погано або не працює:**
- Точні математичні обчислення (використовуйте калькулятор)
- Робота з актуальними даними (модель знає лише те, на чому навчалась)
- Гарантована коректність (модель може "галюцинувати" — видавати вигадане за факт)
- Детерміністичність (один і той же запит може дати різні відповіді)

---

## Чому LLM змінює правила гри для розробників

### До LLM vs Після LLM

**Задача: Витягти ім'я, email та компанію з вільного тексту**

До LLM:
```typescript
// Regex hell: крихкий, ламається на кожному новому форматі
const emailRegex = /([a-zA-Z0-9._-]+@[a-zA-Z0-9._-]+\.[a-zA-Z0-9_-]+)/;
const nameRegex = /(?:my name is|i'm|i am)\s+([A-Z][a-z]+ [A-Z][a-z]+)/i;
// + ще 200 рядків regex для різних форматів...
// + NER-модель яку треба навчити на ваших даних...
// + обробка помилок для кожного edge case...
```

Після LLM:
```typescript
import { generateObject } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

const { object } = await generateObject({
  model: openai('gpt-4o-mini'),  // Або anthropic('claude-haiku'), або google('gemini-flash')
  schema: z.object({
    name: z.string(),
    email: z.string().email(),
    company: z.string(),
  }),
  prompt: `Витягни контактні дані: "${текст}"`,
});
// Працює з будь-яким провайдером — міняється тільки рядок model
```

**Різниця:** замість тижня роботи над regex — 10 рядків коду за 5 хвилин, і результат працює краще.

### Де розробники вже використовують LLM на практиці

| Де працює LLM | Приклад | Бізнес-цінність |
|---------------|---------|-----------------|
| Customer Support | Бот що відповідає по базі знань | Заміна 50-70% запитів до людей |
| Обробка документів | Витягування даних з PDF/рахунків | Автоматизація ручної роботи |
| Code Review | Аналіз PR на помилки та безпеку | Швидший та якісніший рев'ю |
| Контент | Генерація описів товарів, email | Масштабування контент-команди |
| Пошук | Семантичний пошук по документах | Знаходити релевантне, а не ключові слова |
| Аналітика | Класифікація відгуків, тікетів | Автоматичне тегування та маршрутизація |

---

## Ландшафт моделей у 2026 році

### Основні провайдери та їх моделі

**OpenAI (GPT)**

| Модель | Ціна (вхід/вихід за 1M токенів) | Контекст | Коли використовувати |
|--------|--------------------------------|----------|---------------------|
| GPT-5 | $1.25 / $10.00 | 400K | Складні задачі, найвища якість |
| GPT-5 Mini | $0.25 / $2.00 | 400K | Баланс якості і ціни |
| GPT-5 Nano | $0.05 / $0.40 | 400K | Прості задачі, мінімальна ціна |
| GPT-4.1 | $2.00 / $8.00 | 1M | Дуже великі документи |
| GPT-4o-mini | $0.15 / $0.60 | 128K | Найдешевший варіант, прості задачі |
| o3 | $2.00 / $8.00 | 200K | Задачі з міркуванням (математика, логіка) |
| o4-mini | $1.10 / $4.40 | 200K | Дешеве міркування |

**Anthropic (Claude)**

| Модель | Ціна (вхід/вихід за 1M токенів) | Контекст | Коли використовувати |
|--------|--------------------------------|----------|---------------------|
| Claude Opus 4.6 | $5 / $25 | 200K | Найскладніші задачі, агенти |
| Claude Sonnet 4.5 | $3 / $15 | 200K | Робоча конячка — баланс якості і ціни |
| Claude Haiku 4.5 | $1 / $5 | 200K | Швидкі прості задачі |

**Google (Gemini)**

| Модель | Ціна (вхід/вихід за 1M токенів) | Контекст | Коли використовувати |
|--------|--------------------------------|----------|---------------------|
| Gemini 2.5 Pro | $1.25 / $10.00 | 1M | Аналіз великих документів, мультимодальність |
| Gemini 2.5 Flash | $0.30 / $2.50 | 1M | Швидкі задачі з великим контекстом |

**Open Source (безкоштовно, self-hosted)**

| Модель | Параметри | Коли використовувати |
|--------|-----------|---------------------|
| DeepSeek V3.2 | 685B (MoE) | Найдешевша альтернатива frontier моделям |
| Llama 4 Scout | 109B | Self-hosted, контекст 10M токенів |
| Qwen3-235B | 235B | Найпопулярніша open-source модель |

### Як обрати модель: Практичне правило

```
Прості задачі (класифікація, форматування)
→ GPT-4o-mini ($0.15/1M) або Gemini Flash ($0.30/1M)

Типові задачі (генерація тексту, код, чат)
→ Claude Sonnet 4.5 ($3/1M) або GPT-5 Mini ($0.25/1M)

Складні задачі (агенти, складна логіка, аналіз)
→ Claude Opus 4.6 ($5/1M) або GPT-5 ($1.25/1M)

Задачі з міркуванням (математика, планування, дебаг)
→ o3 ($2/1M) або Claude з Extended Thinking

Великі документи (>200K токенів)
→ Gemini 2.5 Pro (1M контекст) або GPT-4.1 (1M контекст)
```

---

## Чому TypeScript, а не Python?

У 2025 році TypeScript обігнав Python за кількістю контриб'юторів на GitHub. Для AI-розробки TypeScript має конкретні переваги:

**Типізація** — Zod-схеми гарантують що LLM повертає дані правильної структури. У Python це `pydantic`, але екосистема менш зручна.

**Один стек** — Front-end, back-end, та AI-логіка однією мовою. Стрімінг відповідей LLM у React через AI SDK — це буквально один рядок.

**Vercel AI SDK** — найпопулярніший AI SDK у світі (2.8M завантажень/тиждень), і він TypeScript-first.

**Всі major frameworks** тепер мають TypeScript SDK:
- OpenAI Agents SDK (`@openai/agents`)
- Claude Agent SDK (`@anthropic-ai/claude-agent-sdk`)
- Google ADK (`adk-js`)
- LangGraph.js
- Mastra

---

## Налаштування середовища

### Крок 1: Встановлення базових інструментів

```bash
# Переконайтесь що у вас Node.js 22+
node --version  # має бути v22.x або вище

# Створіть проект для курсу
mkdir llm-course && cd llm-course
npm init -y

# Встановіть основні залежності
npm install ai @ai-sdk/openai @ai-sdk/anthropic @ai-sdk/google zod dotenv
npm install -D typescript tsx @types/node

# Ініціалізуйте TypeScript
npx tsc --init --target es2022 --module nodenext --moduleResolution nodenext
```

### Крок 2: API ключі

Вам потрібен хоча б один API ключ. Рекомендуємо почати з OpenAI або Google AI (безкоштовний tier):

| Провайдер | Де отримати | Безкоштовний tier |
|-----------|------------|-------------------|
| OpenAI | [platform.openai.com](https://platform.openai.com) | $5 кредитів для нових акаунтів |
| Anthropic | [console.anthropic.com](https://console.anthropic.com) | $5 кредитів для нових акаунтів |
| Google AI | [aistudio.google.com](https://aistudio.google.com) | Безкоштовно до 60 запитів/хв |

```bash
# Створіть файл .env (НЕ комітьте його в git!)
cat > .env << 'EOF'
OPENAI_API_KEY=sk-your-key-here
ANTHROPIC_API_KEY=sk-ant-your-key-here
GOOGLE_GENERATIVE_AI_API_KEY=your-key-here
EOF

# Додайте .env в .gitignore
echo ".env" >> .gitignore
```

### Крок 3: Hello World — перевіряємо що все працює

Створіть файл `hello.ts`:

```typescript
import 'dotenv/config';
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';

async function main() {
  const { text, usage } = await generateText({
    model: openai('gpt-4o-mini'),
    prompt: 'Поясни що таке TypeScript одним реченням.',
  });

  console.log('Відповідь:', text);
  console.log('Використано токенів:', usage.totalTokens);
  console.log('Орієнтовна вартість:', `$${(usage.totalTokens * 0.00000015).toFixed(6)}`);
}

main().catch(console.error);
```

```bash
npx tsx hello.ts
```

Очікуваний вивід:
```
Відповідь: TypeScript — це надмножина JavaScript з опціональною статичною типізацією, що допомагає виявляти помилки під час розробки.
Використано токенів: 52
Орієнтовна вартість: $0.000008
```

Якщо ви бачите відповідь — все працює, можна переходити до наступного модуля.

### Troubleshooting

**`OPENAI_API_KEY is not set`** — перевірте що файл `.env` існує і містить ключ. Перевірте що ви імпортуєте `dotenv/config`.

**`401 Unauthorized`** — ключ невалідний або прострочений. Згенеруйте новий на сайті провайдера.

**`429 Rate limit exceeded`** — занадто багато запитів. Зачекайте хвилину і спробуйте знову.

**`insufficient_quota`** — закінчились кредити. Поповніть баланс або використайте безкоштовний Google AI.

---

## Перевір себе

1. Що таке LLM і чим воно відрізняється від звичайної програми?
2. Назвіть 3 задачі де LLM працює добре і 3 де погано
3. Яку модель ви б обрали для класифікації тисяч повідомлень? Чому?
4. Чому TypeScript став популярним для AI-розробки?
5. Запустіть hello.ts і отримайте відповідь від LLM

---

**Далі:** [Модуль 01 — Базові поняття: Токени, контекст, моделі →](01-basics.md)
