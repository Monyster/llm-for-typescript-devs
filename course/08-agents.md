# Модуль 08: AI Агенти — Автономні програми з LLM-мозком

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Розуміти чим агент відрізняється від чатбота та простого tool call
- Знати патерн ReAct (Reason + Act) та agent loop
- Будувати агента який самостійно планує та виконує задачі за кілька кроків
- Визначати критерії зупинки агента

**Які задачі це дозволяє вирішувати:** Створити AI-систему яка самостійно: досліджує тему в інтернеті, аналізує дані, робить висновки, виконує дії через API. Наприклад: агент підтримки що сам шукає відповідь в базі знань, перевіряє замовлення, повертає гроші — без участі людини.

---

## 8.1 Агент vs Чатбот vs Tool Calling

```
Чатбот:        Запит → LLM → Відповідь (1 крок)

Tool Calling:  Запит → LLM → Tool → LLM → Відповідь (2-3 кроки, ви контролюєте)

Агент:         Запит → LLM → Tool → LLM → Tool → LLM → Tool → ... → Відповідь
               (N кроків, LLM сам вирішує що робити далі)
```

**Агент** — це LLM яка в циклі: ДУМАЄ → ДІЯЄ → СПОСТЕРІГАЄ результат → ДУМАЄ знову. І так поки не вирішить задачу або не досягне ліміту кроків.

### ReAct Pattern (Reason + Act)

```
Крок 1: REASON — "Для відповіді на це питання мені потрібно знайти замовлення клієнта"
         ACT    — викликаю searchOrders({ email: "user@test.com" })
         OBSERVE — знайдено 3 замовлення

Крок 2: REASON — "Клієнт питає про останнє замовлення, це #456"
         ACT    — викликаю getOrderDetails({ id: "456" })
         OBSERVE — замовлення відправлено, трекінг: UA123456

Крок 3: REASON — "Тепер маю всю інформацію, можу відповісти"
         ACT    — формую фінальну відповідь
```

---

## 8.2 Агент з AI SDK

```typescript
import { generateText, tool } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

const { text, steps } = await generateText({
  model: openai('gpt-4o-mini'),
  system: `Ти — агент підтримки інтернет-магазину.
Ти маєш доступ до інструментів для пошуку замовлень та інформації.
Збирай інформацію покроково, поки не зможеш дати повну відповідь.
Будь ввічливим та конкретним.`,

  tools: {
    searchOrders: tool({
      description: 'Пошук замовлень клієнта за email або номером телефону',
      parameters: z.object({
        email: z.string().optional(),
        phone: z.string().optional(),
      }),
      execute: async ({ email, phone }) => {
        return db.orders.search({ email, phone });
      },
    }),

    getOrderDetails: tool({
      description: 'Детальна інформація про замовлення за ID',
      parameters: z.object({ orderId: z.string() }),
      execute: async ({ orderId }) => {
        return db.orders.findById(orderId);
      },
    }),

    getShippingStatus: tool({
      description: 'Статус доставки за трекінг-номером',
      parameters: z.object({ trackingNumber: z.string() }),
      execute: async ({ trackingNumber }) => {
        return shipping.getStatus(trackingNumber);
      },
    }),

    initiateRefund: tool({
      description: 'Ініціювати повернення коштів за замовлення',
      parameters: z.object({
        orderId: z.string(),
        reason: z.string(),
      }),
      execute: async ({ orderId, reason }) => {
        return payments.refund(orderId, reason);
      },
    }),
  },

  maxSteps: 10,  // Максимум 10 кроків
  prompt: 'Привіт, я замовляв навушники тиждень тому, email oleg@test.com. Де моє замовлення?',
});

// Переглянути кроки агента
for (const step of steps) {
  console.log(`Крок ${step.stepType}:`, step.toolCalls?.map(tc => tc.toolName));
}
console.log('\nВідповідь:', text);
```

---

## 8.3 Критерії зупинки агента

Агент може зациклитися або витратити занадто багато грошей. Обов'язково встановлюйте ліміти:

```typescript
const MAX_STEPS = 10;       // Максимум кроків
const MAX_TOKENS = 50_000;  // Максимум токенів за сесію
const MAX_COST = 0.50;      // Максимум $0.50 за сесію
const TIMEOUT_MS = 60_000;  // Таймаут 60 секунд

const { text, usage, steps } = await generateText({
  model: openai('gpt-4o-mini'),
  tools: { /* ... */ },
  maxSteps: MAX_STEPS,
  maxTokens: 4096,
  abortSignal: AbortSignal.timeout(TIMEOUT_MS),
  prompt: userMessage,
});

// Перевірка вартості
const cost = (usage.promptTokens * 0.15 + usage.completionTokens * 0.6) / 1_000_000;
if (cost > MAX_COST) {
  console.warn(`Агент витратив $${cost.toFixed(4)} — перевищено ліміт`);
}

console.log(`Кроків: ${steps.length}, Токенів: ${usage.totalTokens}, Вартість: $${cost.toFixed(4)}`);
```

---

## 8.4 Архітектура production-ready агента

```typescript
// agent.ts — структура production агента
interface AgentConfig {
  model: LanguageModel;
  systemPrompt: string;
  tools: Record<string, Tool>;
  maxSteps: number;
  maxCostPerSession: number;
  onStepComplete?: (step: StepInfo) => void;  // Логування
  onError?: (error: Error) => void;
}

async function runAgent(config: AgentConfig, userMessage: string) {
  const startTime = Date.now();

  try {
    const { text, steps, usage } = await generateText({
      model: config.model,
      system: config.systemPrompt,
      tools: config.tools,
      maxSteps: config.maxSteps,
      prompt: userMessage,
    });

    // Логуємо кожен крок
    for (const step of steps) {
      config.onStepComplete?.({
        toolCalls: step.toolCalls,
        tokens: step.usage?.totalTokens,
        duration: Date.now() - startTime,
      });
    }

    return {
      response: text,
      steps: steps.length,
      tokens: usage.totalTokens,
      cost: calculateCost(usage),
      duration: Date.now() - startTime,
    };
  } catch (error) {
    config.onError?.(error as Error);
    return { response: 'Вибачте, сталася помилка. Спробуйте ще раз.', error };
  }
}
```

---

## 8.4.1 Агенти з різними провайдерами

Через AI SDK агенти працюють з будь-яким провайдером — але є нюанси:

```typescript
const agentConfig = {
  system: 'Ти — дослідницький агент. Використовуй tools для збору інформації.',
  tools: { search: searchTool, analyze: analyzeTool, summarize: summarizeTool },
  maxSteps: 8,
  prompt: 'Досліди тренди TypeScript фреймворків у 2026',
};

// GPT-4o-mini — найдешевший, добре обирає tools, іноді "забуває" зупинитись
await generateText({ model: openai('gpt-4o-mini'), ...agentConfig });

// Claude Sonnet — найкраще слідує складним інструкціям, рідко зациклюється
await generateText({ model: anthropic('claude-sonnet-4-5-20250929'), ...agentConfig });

// Gemini Flash — найшвидший, але іноді пропускає tools і відповідає напряму
await generateText({ model: google('gemini-2.5-flash-preview-04-17'), ...agentConfig });
```

**Практичні відмінності при agent loop:**

| Поведінка | OpenAI GPT-4o-mini | Claude Sonnet | Gemini Flash |
|-----------|-------------------|---------------|--------------|
| Якість вибору tool | Добра | **Найкраща** | Добра |
| Паралельні tool calls | ✅ Часто робить | ✅ Часто робить | ❌ По одному |
| Слідування system prompt | Добре | **Найкраще** | Добре |
| Схильність до зациклювання | Середня | Низька | Низька |
| Швидкість (per step) | ~500ms | ~800ms | **~300ms** |
| Вартість (8 steps) | ~$0.005 | ~$0.02 | ~$0.003 |

**Порада:** Для production-агентів часто вигідна комбінація — Claude для складних рішень, GPT-4o-mini для простих tool calls:

```typescript
// Model routing для агента
function getAgentModel(complexity: 'simple' | 'complex') {
  return complexity === 'complex'
    ? anthropic('claude-sonnet-4-5-20250929')  // Складні multi-step задачі
    : openai('gpt-4o-mini');                     // Прості tool call цикли
}
```

---

## 8.5 Коли використовувати агентів, а коли ні

| Задача | Агент потрібен? | Чому |
|--------|----------------|------|
| Відповіді з бази знань | Ні — достатньо RAG | Один крок: пошук + відповідь |
| Обробка замовлення | Так | Кілька кроків: перевірка, оплата, email |
| Класифікація тексту | Ні | Один виклик generateObject |
| Дослідження теми | Так | Багато кроків: пошук, аналіз, порівняння |
| Генерація звіту з кількох джерел | Так | Збір даних з різних API |
| Переклад тексту | Ні | Один виклик generateText |

**Правило:** Якщо задачу можна вирішити за 1-2 кроки — не робіть агента. Агент = складність + вартість + непередбачуваність.

---

## Перевір себе

1. Чим агент принципово відрізняється від tool calling?
2. Що таке ReAct pattern? Назвіть три фази.
3. Навіщо потрібні `maxSteps`, таймаут і ліміт вартості?
4. Створіть агента з 3 tools для задачі планування подорожі
5. Назвіть 3 задачі де агент НЕ потрібен та поясніть чому

---

**Назад:** [← Модуль 07 — Стрімінг та UI](07-streaming.md) | **Далі:** [Модуль 09 — Пам'ять агентів →](09-memory.md)
