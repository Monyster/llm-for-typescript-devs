# Модуль 01: Базові поняття — Токени, контекст, моделі

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Розуміти що таке токени і як вони впливають на вартість
- Знати що таке контекстне вікно і чому воно обмежує ваш застосунок
- Розуміти різницю між температурою 0 і температурою 1
- Вміти оцінювати вартість будь-якого LLM-запиту до його відправки

**Які задачі це дозволяє вирішувати:** Оцінювати бюджет на AI, обирати модель під конкретну задачу, розуміти чому модель "забуває" початок розмови, і чому іноді відповіді різні на один і той же запит.

---

## 💰 Бізнес-цінність

**Проблема клієнта:** "Чому один запит коштує $0.001 а інший $0.50?"
**Рішення з цього модуля:** Ви розумієте токени, моделі, ціноутворення. Можете оцінити вартість AI-рішення до початку розробки.
**Як продати:** Розрахунок вартості AI-рішення для клієнта — частина кожного proposal. Клієнт платить за прогнозованість.

## 1.1 Токени — одиниця вимірювання LLM

### Що таке токен

Токен — це **фрагмент тексту**, який модель обробляє як одну одиницю. Це НЕ слово і НЕ символ. Це щось між ними.

Приблизне правило:
- **Англійська:** 1 токен ≈ 0.75 слова (або ~4 символи)
- **Українська:** 1 токен ≈ 0.5 слова (кирилиця дорожча)
- **Код:** залежить від мови, TypeScript ≈ 0.6 слова/токен

### Практичний експеримент

```typescript
import 'dotenv/config';
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';

async function countTokens() {
  // Однаковий зміст, різна кількість токенів
  const texts = [
    { lang: 'English', text: 'Hello, how are you doing today?' },
    { lang: 'Ukrainian', text: 'Привіт, як у тебе справи сьогодні?' },
    { lang: 'TypeScript', text: 'const greet = (name: string): string => `Hello ${name}`;' },
  ];

  for (const { lang, text } of texts) {
    const { usage } = await generateText({
      model: openai('gpt-4o-mini'),
      prompt: `Repeat this text exactly: "${text}"`,
    });
    console.log(`${lang}: "${text}"`);
    console.log(`  Токенів: ${usage.totalTokens}\n`);
  }
}

countTokens();
```

### Чому це важливо

Все в LLM вимірюється в токенах:
- **Ціна** — ви платите за кількість токенів (вхід + вихід)
- **Ліміти** — контекстне вікно обмежене в токенах
- **Швидкість** — модель генерує ~50-150 токенів/секунду

### Розрахунок вартості

Формула проста:

```
Вартість = (вхідні_токени × ціна_входу) + (вихідні_токени × ціна_виходу)
```

Приклад для GPT-4o-mini ($0.15 вхід / $0.60 вихід за 1M токенів):

```typescript
function estimateCost(inputTokens: number, outputTokens: number) {
  const inputCost = (inputTokens / 1_000_000) * 0.15;
  const outputCost = (outputTokens / 1_000_000) * 0.60;
  return { inputCost, outputCost, total: inputCost + outputCost };
}

// Типовий чат-запит: ~200 вхідних, ~300 вихідних токенів
console.log(estimateCost(200, 300));
// { inputCost: 0.00003, outputCost: 0.00018, total: 0.00021 }
// ~$0.0002 за один запит
// ~5,000 запитів за $1
```

### Порівняння вартості моделей

| Задача | Токенів (вхід/вихід) | GPT-4o-mini | Claude Sonnet 4.5 | GPT-5 |
|--------|---------------------|-------------|-------------------|-------|
| Класифікація повідомлення | 100/10 | $0.00002 | $0.0005 | $0.0001 |
| Генерація email | 500/400 | $0.0003 | $0.008 | $0.005 |
| Аналіз документа (10 стор.) | 5000/1000 | $0.001 | $0.03 | $0.01 |
| Повна сторінка коду | 2000/3000 | $0.002 | $0.05 | $0.03 |

**Висновок:** для простих задач різниця в ціні — 100x. Тому вибір моделі — це інженерне рішення, а не "беремо найрозумнішу".

---

## 1.2 Контекстне вікно — пам'ять моделі

### Що це таке

Контекстне вікно (context window) — це **максимальний обсяг тексту**, який модель може "бачити" за один раз. Це сума вхідних І вихідних токенів.

```
┌─────────────────────────────────────────────────────┐
│                 Context Window (128K)                │
│                                                     │
│  [System Prompt]  [Історія чату]  [Новий запит]     │
│  ~~~1000 tok~~~  ~~~50000 tok~~~ ~~~500 tok~~~~     │
│                                                     │
│  Залишилось для відповіді: 128000 - 51500 = 76500   │
└─────────────────────────────────────────────────────┘
```

### Розміри контекстних вікон

| Модель | Контекст | Скільки це приблизно |
|--------|----------|---------------------|
| GPT-4o-mini | 128K | ~200 сторінок тексту |
| Claude Sonnet 4.5 | 200K | ~300 сторінок |
| GPT-5 | 400K | ~600 сторінок |
| Gemini 2.5 Pro | 1M | ~1500 сторінок |
| Llama 4 Scout | 10M | ~15000 сторінок |

### Коли контекстне вікно стає проблемою

**Проблема 1: Довга розмова**

Кожне повідомлення в чаті займає місце в контекстному вікні. Після 50-100 повідомлень — вікно заповнюється.

```typescript
// Кожне повідомлення додається до контексту
const messages = [
  { role: 'system', content: systemPrompt },      // ~500 токенів
  { role: 'user', content: 'Привіт!' },           // ~5 токенів  
  { role: 'assistant', content: '...' },           // ~100 токенів
  { role: 'user', content: 'Розкажи про...' },    // ~20 токенів
  { role: 'assistant', content: '...' },           // ~500 токенів
  // ... через 100 повідомлень = ~30,000 токенів
  // І ви платите за ВСІ ці токени з КОЖНИМ запитом!
];
```

**Проблема 2: "Забуває" початок**

Навіть якщо текст поміщається у вікно, моделі мають тенденцію гірше "пам'ятати" інформацію з середини довгого контексту. Це називається "Lost in the Middle" проблема.

**Рішення** (ви їх вивчите в наступних модулях):
- Summarization — стискати стару історію
- RAG — тримати документи в базі і підтягувати лише релевантні
- Sliding Window — відкидати найстаріші повідомлення

---

## 1.3 Температура та інші параметри генерації

### Temperature

Temperature контролює **наскільки "креативною" буде відповідь**:

- **temperature: 0** — модель завжди обирає найімовірніше слово. Результат максимально передбачуваний і повторюваний.
- **temperature: 0.7** — помірна варіативність. Хороший баланс для більшості задач.
- **temperature: 1.0** — максимальна креативність. Кожен запуск дає різний результат.

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';

// Завжди однаковий результат — для парсингу даних, класифікації
const precise = await generateText({
  model: openai('gpt-4o-mini'),
  temperature: 0,
  prompt: 'Класифікуй повідомлення "Мій замовлення не прийшло": support/sales/general',
});

// Різний результат кожен раз — для генерації контенту
const creative = await generateText({
  model: openai('gpt-4o-mini'),
  temperature: 0.9,
  prompt: 'Придумай назву для TypeScript-бібліотеки для роботи з AI',
});
```

### Практичне правило вибору temperature

| Задача | Temperature | Чому |
|--------|------------|------|
| Витягування даних (парсинг) | 0 | Потрібна точність і повторюваність |
| Класифікація | 0 | Один і той же текст → одна категорія |
| Відповіді на питання по документах | 0-0.3 | Фактичність важливіша за креативність |
| Чатбот для підтримки | 0.3-0.5 | Трохи варіативності, але не галюцинації |
| Генерація email/тексту | 0.5-0.7 | Природність мови |
| Креативне письмо | 0.8-1.0 | Максимальна різноманітність |
| Генерація коду | 0-0.2 | Код має бути робочим, не "креативним" |

### Max Tokens (maxTokens)

Обмежує **максимальну довжину відповіді**:

```typescript
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  maxTokens: 100,  // Модель зупиниться після ~100 токенів
  prompt: 'Поясни квантову фізику',
});
// Відповідь буде обрізана. Корисно для контролю вартості.
```

**Коли встановлювати:**
- Класифікація: `maxTokens: 10` (потрібно одне слово)
- Короткий підсумок: `maxTokens: 200`
- Генерація статті: `maxTokens: 2000`
- Не обмежувати: не вказуйте параметр (або `maxTokens: 4096`)

### System Prompt (role: 'system')

System prompt — це **інструкція для моделі**, яка задає її поведінку на всю розмову:

```typescript
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  system: `Ти — senior TypeScript розробник з 10 роками досвіду.
Відповідай коротко і по суті.
Завжди показуй приклади коду.
Код пиши тільки на TypeScript.`,
  prompt: 'Як зробити HTTP запит?',
});
```

System prompt — це **найважливіший** параметр. Він визначає:
- Роль і тон моделі
- Формат відповідей
- Обмеження (чого модель не повинна робити)
- Контекст (яка інформація доступна)

---

## 1.4 Reasoning Models: Коли модель "думає"

### Що це таке

Reasoning models (o3, o4-mini, Claude з Extended Thinking) — це моделі, які **"міркують" перед відповіддю**. Вони витрачають додаткові токени на внутрішній ланцюг роздумів, але дають значно кращі результати на складних задачах.

**Аналогія:** Звичайна модель — це як відповідати на іспиті миттєво. Reasoning модель — це як написати чернетку, перевірити логіку, і тоді дати фінальну відповідь.

### Коли використовувати reasoning

| Задача | Звичайна модель | Reasoning модель |
|--------|----------------|-----------------|
| "Що таке TypeScript?" | ✅ Достатньо | ❌ Зайво |
| Класифікація тексту | ✅ Достатньо | ❌ Зайво |
| Складний баг в коді | ⚠️ Може помилитись | ✅ Набагато краще |
| Математична задача | ⚠️ Часто помиляється | ✅ Значно точніше |
| Планування архітектури | ⚠️ Поверхнево | ✅ Глибший аналіз |
| Аналіз з кількох кроків | ⚠️ Пропускає кроки | ✅ Послідовна логіка |

### Як використовувати в коді

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { anthropic } from '@ai-sdk/anthropic';
import { google } from '@ai-sdk/google';

// OpenAI o3 — reasoning через окрему модель
const { text: o3Result } = await generateText({
  model: openai('o3'),
  prompt: 'Знайди баг в цьому коді та поясни покроково...',
  // o3 "думає" автоматично, reasoning_effort можна налаштувати
});

// Anthropic Claude — Extended Thinking як опція до звичайної моделі
const { text: claudeResult } = await generateText({
  model: anthropic('claude-sonnet-4-5-20250929'),
  prompt: 'Проаналізуй архітектуру цієї системи...',
  providerOptions: {
    anthropic: {
      thinking: { type: 'enabled', budgetTokens: 10000 },  // Бюджет на "думання"
    },
  },
});

// Google Gemini — thinking як опція (аналогічно до Anthropic)
const { text: geminiResult } = await generateText({
  model: google('gemini-2.5-flash-preview-04-17'),
  prompt: 'Оптимізуй цей SQL запит...',
  providerOptions: {
    google: {
      thinkingConfig: { thinkingBudget: 10000 },
    },
  },
});
```

### Ключова різниця в підходах

| Аспект | OpenAI (o3/o4-mini) | Anthropic (Extended Thinking) | Google (Thinking) |
|--------|-------------------|-------------------------------|-------------------|
| Як увімкнути | Окрема модель (`o3`) | Опція до звичайної моделі | Опція до звичайної моделі |
| Контроль бюджету | `reasoning_effort: low/medium/high` | `budgetTokens: N` (точний) | `thinkingBudget: N` |
| Thinking видно? | Ні (приховано) | Так (в `reasoning` блоках) | Так |
| Temperature | Не підтримує (завжди 1) | Підтримує | Підтримує |
| Вартість thinking | Оплачується як output | Оплачується як output | Оплачується як output |

**Важливо:** reasoning моделі дорожчі (токени на "роздуми" теж оплачуються) і повільніші. Використовуйте їх тільки коли якість відповіді дійсно критична.

---

## 1.5 Мультимодальність: Не тільки текст

Сучасні моделі працюють не лише з текстом:

| Модальність | Підтримка | Приклад задачі |
|------------|-----------|----------------|
| **Текст → Текст** | Всі моделі | Чат, генерація, аналіз |
| **Зображення → Текст** | GPT-5, Claude, Gemini | Аналіз скріншотів, OCR, опис фото |
| **PDF → Текст** | Claude, Gemini | Витягування даних з документів |
| **Аудіо → Текст** | GPT-5, Gemini | Транскрипція, аналіз голосу |
| **Текст → Зображення** | DALL-E 3, Midjourney | Генерація ілюстрацій |
| **Текст → Аудіо** | OpenAI TTS, ElevenLabs | Озвучка, голосові асистенти |

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { readFileSync } from 'fs';

// Аналіз зображення
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  messages: [
    {
      role: 'user',
      content: [
        { type: 'text', text: 'Що на цьому скріншоті? Є помилки в UI?' },
        {
          type: 'image',
          image: readFileSync('./screenshot.png'),
        },
      ],
    },
  ],
});
```

---

## Перевір себе

1. Скільки токенів приблизно у фразі "Привіт, як справи?" (українською)?
2. Ви відправляєте 1000 запитів на день, кожен ~500 вхідних / 200 вихідних токенів. Порахуйте місячну вартість для GPT-4o-mini і для Claude Sonnet 4.5.
3. У вас чат де після 50 повідомлень модель починає "забувати" початок розмови. Чому це відбувається?
4. Яку temperature ви поставите для: а) парсинг JSON з PDF, б) генерація описів товарів, в) класифікація тікетів?
5. Коли варто використовувати reasoning модель (o3) замість звичайної (GPT-5 Mini)?

---

**Назад:** [← Модуль 00 — Вступ](00-intro.md) | **Далі:** [Модуль 02 — Chat API на практиці →](02-chat-api.md)
