# Модуль 03: Vercel AI SDK — Єдиний інтерфейс до всіх моделей

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Знати архітектуру AI SDK 6: core, providers, UI
- Вміти використовувати `generateText`, `streamText`, `generateObject`, `streamObject`
- Розуміти систему провайдерів та як додавати нові моделі
- Вміти переключатися між моделями без зміни коду

**Які задачі це дозволяє вирішувати:** Будувати AI-застосунки, які не прив'язані до одного провайдера. Легко мігрувати між моделями, використовувати найдешевшу модель для кожної задачі, та мати fallback при відмові провайдера.

---

## 💰 Бізнес-цінність

**Проблема клієнта:** "Ми почали з OpenAI, але хочемо мати можливість перейти на Claude/Bedrock."
**Рішення з цього модуля:** AI SDK дозволяє змінити провайдера однією зміною рядка коду. Нуль vendor lock-in.
**Як продати:** "Архітектура без залежності від одного провайдера" — аргумент для enterprise. Додає $2–5K до вартості проекту через зменшення ризику.

## 3.1 Архітектура AI SDK 6

AI SDK складається з трьох частин:

```
┌───────────────────────────────────────────────────────┐
│                    Ваш Застосунок                     │
├───────────────────────────────────────────────────────┤
│                  AI SDK Core                          │
│  generateText · streamText · generateObject · embed   │
├───────────────────────────────────────────────────────┤
│                  AI SDK Providers                     │
│  @ai-sdk/openai · @ai-sdk/anthropic · @ai-sdk/google │
├───────────────────────────────────────────────────────┤
│                  AI SDK UI (опціонально)              │
│  useChat · useCompletion · useObject (React hooks)    │
└───────────────────────────────────────────────────────┘
```

**Core** — основні функції для роботи з LLM. Не залежить від провайдера.

**Providers** — адаптери для конкретних API (OpenAI, Anthropic, Google, та 25+ інших).

**UI** — React hooks для побудови чат-інтерфейсів (опціонально).

### Встановлення

```bash
# Core + провайдери які вам потрібні
npm install ai @ai-sdk/openai @ai-sdk/anthropic @ai-sdk/google zod
```

---

## 3.2 Основні функції

### generateText — отримати повну відповідь

Найпростіша функція. Відправляє запит, чекає повну відповідь:

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';

const { text, usage, finishReason } = await generateText({
  model: openai('gpt-4o-mini'),
  system: 'Ти — технічний письменник.',
  prompt: 'Напиши JSDoc для функції сортування масиву',
  temperature: 0,
  maxTokens: 500,
});
```

**Коли використовувати:** Коли вам потрібна повна відповідь перед обробкою — парсинг, витягування даних, генерація файлів.

### streamText — стрімінг по частинах

Відповідь приходить по токенах. Користувач бачить текст одразу:

```typescript
import { streamText } from 'ai';
import { openai } from '@ai-sdk/openai';

const result = streamText({
  model: openai('gpt-4o-mini'),
  prompt: 'Поясни async/await в TypeScript',
});

// Варіант 1: потоковий вивід в консоль
for await (const chunk of result.textStream) {
  process.stdout.write(chunk);
}

// Варіант 2: отримати повний текст після завершення
const fullText = await result.text;
const usage = await result.usage;
```

**Коли використовувати:** Чатботи, UI де потрібен миттєвий відгук, довгі відповіді.

### generateObject — отримати типізований об'єкт

Модель повертає JSON який автоматично валідується через Zod:

```typescript
import { generateObject } from 'ai';
import { openai } from '@ai-sdk/openai';
import { z } from 'zod';

const { object } = await generateObject({
  model: openai('gpt-4o-mini'),
  schema: z.object({
    sentiment: z.enum(['positive', 'negative', 'neutral']),
    confidence: z.number().min(0).max(1),
    topics: z.array(z.string()).max(5),
    summary: z.string().max(100),
  }),
  prompt: 'Проаналізуй відгук: "Чудовий продукт, але доставка занадто довга"',
});

console.log(object);
// {
//   sentiment: 'positive',
//   confidence: 0.65,
//   topics: ['product quality', 'delivery'],
//   summary: 'Позитивний відгук про продукт з зауваженням щодо доставки'
// }
```

**Коли використовувати:** Витягування даних, класифікація, будь-яка задача де потрібен структурований результат. Детально — у Модулі 05.

### embed — отримати вектор тексту

Перетворює текст на числовий вектор для семантичного пошуку:

```typescript
import { embed, embedMany } from 'ai';
import { openai } from '@ai-sdk/openai';

// Один текст → один вектор
const { embedding } = await embed({
  model: openai.embedding('text-embedding-3-small'),
  value: 'TypeScript — це надмножина JavaScript з типізацією',
});
// embedding: [0.023, -0.041, 0.087, ...] — масив з 1536 чисел

// Багато текстів → багато векторів (пакетно, дешевше)
const { embeddings } = await embedMany({
  model: openai.embedding('text-embedding-3-small'),
  values: [
    'Як створити REST API на Node.js?',
    'Що таке Docker і навіщо він потрібен?',
    'Найкращі практики TypeScript у 2026',
  ],
});
```

**Коли використовувати:** RAG (пошук по документах), семантичний пошук, кластеризація текстів. Детально — у Модулі 10.

---

## 3.3 Система провайдерів

### Легке переключення між моделями

```typescript
import { openai } from '@ai-sdk/openai';
import { anthropic } from '@ai-sdk/anthropic';
import { google } from '@ai-sdk/google';
import { LanguageModel } from 'ai';

// Обирайте модель через конфігурацію, а не хардкод
function getModel(task: 'cheap' | 'smart' | 'fast'): LanguageModel {
  switch (task) {
    case 'cheap':
      return openai('gpt-4o-mini');          // $0.15/1M — для простих задач
    case 'smart':
      return anthropic('claude-sonnet-4-5-20250929'); // $3/1M — для складних
    case 'fast':
      return google('gemini-2.5-flash-preview-04-17'); // Швидкий + 1M контекст
  }
}

// Код не залежить від провайдера
const { text } = await generateText({
  model: getModel('cheap'),
  prompt: 'Класифікуй текст...',
});
```

### 🆕 Enterprise провайдери: Bedrock і Azure

Ті ж моделі, але через enterprise cloud — для клієнтів з compliance-вимогами:

```bash
npm install @ai-sdk/amazon-bedrock  # AWS Bedrock
npm install @ai-sdk/azure           # Azure OpenAI
npm install @ai-sdk/google-vertex   # Google Vertex AI
```

```typescript
import { bedrock } from '@ai-sdk/amazon-bedrock';
import { azure } from '@ai-sdk/azure';

// Claude через AWS Bedrock — дані залишаються в AWS клієнта
const { text: bedrockResult } = await generateText({
  model: bedrock('us.anthropic.claude-sonnet-4-5-20250929-v1:0'),
  prompt: 'Проаналізуй...',
});

// GPT через Azure — дані залишаються в Azure клієнта
const { text: azureResult } = await generateText({
  model: azure('your-gpt5-deployment'),
  prompt: 'Summarize...',
});

// Код ІДЕНТИЧНИЙ — змінюється тільки model
```

Бізнес-логіка не змінюється при переході між провайдерами. Детальніше: [Enterprise Providers](enterprise-providers.md).

### AI Gateway: 100+ моделей через один рядок

AI SDK 6 інтегрується з Vercel AI Gateway для доступу до будь-якої моделі через простий ідентифікатор:

```typescript
import { gateway } from '@ai-sdk/gateway';

// Будь-яка модель через строковий ідентифікатор
const { text } = await generateText({
  model: gateway('anthropic/claude-sonnet-4-5'),
  prompt: '...',
});

// Легко переключитись:
// gateway('openai/gpt-5')
// gateway('google/gemini-2.5-pro')
// gateway('meta/llama-4-scout')
```

---

## 3.4 Конфігурація провайдерів

### Кастомні налаштування

```typescript
import { createOpenAI } from '@ai-sdk/openai';

// Кастомний провайдер (наприклад, Azure OpenAI або свій проксі)
const customOpenAI = createOpenAI({
  apiKey: process.env.AZURE_OPENAI_KEY,
  baseURL: 'https://your-resource.openai.azure.com/openai/deployments/gpt-4o',
  headers: {
    'api-version': '2024-12-01-preview',
  },
});

const { text } = await generateText({
  model: customOpenAI('gpt-4o'),
  prompt: '...',
});
```

### Fallback між провайдерами

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { anthropic } from '@ai-sdk/anthropic';

async function generateWithFallback(prompt: string): Promise<string> {
  const providers = [
    () => openai('gpt-4o-mini'),
    () => anthropic('claude-haiku-4-5-20251001'),
    () => openai('gpt-5-nano'),  // Найдешевший fallback
  ];

  for (const getModel of providers) {
    try {
      const { text } = await generateText({
        model: getModel(),
        prompt,
        maxRetries: 2,
      });
      return text;
    } catch (error) {
      console.warn(`Провайдер відмовив: ${error.message}. Пробуємо наступний...`);
    }
  }

  throw new Error('Всі провайдери недоступні');
}
```

---

## 3.5 Messages API vs Prompt API

AI SDK підтримує два способи формування запиту:

```typescript
// Спосіб 1: prompt (простий — для одноразових запитів)
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  system: 'Ти — TypeScript-експерт.',
  prompt: 'Що таке utility types?',
});

// Спосіб 2: messages (діалог — для чатів)
const { text: text2 } = await generateText({
  model: openai('gpt-4o-mini'),
  messages: [
    { role: 'system', content: 'Ти — TypeScript-експерт.' },
    { role: 'user', content: 'Що таке utility types?' },
    { role: 'assistant', content: 'Utility types — це вбудовані типи TypeScript...' },
    { role: 'user', content: 'Покажи приклад з Partial' },
  ],
});
```

**Правило:** Використовуйте `prompt` для одноразових задач (парсинг, класифікація). Використовуйте `messages` для діалогів та коли потрібен контекст попередніх повідомлень.

---

## Перевір себе

1. Назвіть 4 основні функції AI SDK Core та коли кожну використовувати
2. Напишіть код який використовує `generateText` з Anthropic замість OpenAI (змініть лише 2 рядки)
3. Чим `streamText` відрізняється від `generateText`? Коли що краще?
4. Реалізуйте функцію яка спочатку пробує Claude, а при помилці — GPT-4o-mini
5. Що таке AI Gateway і яку проблему він вирішує?

---

**Назад:** [← Модуль 02 — Chat API](02-chat-api.md) | **Далі:** [Модуль 04 — Промпт-інженерія →](04-prompting.md)
