# Модуль 11: MCP — Стандартний протокол для інструментів AI

## 🎯 Що ви отримаєте з цього модуля

Після проходження ви будете:
- Розуміти що таке MCP і чим він відрізняється від звичайного REST API
- Підключати готові MCP-сервери до своїх AI-застосунків
- Створювати власні MCP-сервери для своїх даних та інструментів
- Знати про безпеку MCP та як уникнути supply chain атак

**Які задачі це дозволяє вирішувати:** Підключити свого агента до GitHub, Slack, баз даних, файлової системи, Jira, Google Drive — через стандартний протокол. Створити свій MCP-сервер щоб інші AI-інструменти (Claude, Cursor, VS Code) могли працювати з вашими даними.

---

## 11.1 Що таке MCP і навіщо він потрібен

**MCP (Model Context Protocol)** — це відкритий стандарт для підключення AI-моделей до зовнішніх джерел даних та інструментів. Створений Anthropic у листопаді 2024, з грудня 2025 — під управлінням Linux Foundation.

### Аналогія

MCP — це як **USB для AI**. Як USB дозволяє підключити будь-який пристрій до комп'ютера через один стандарт, MCP дозволяє підключити будь-яке джерело даних до AI через один протокол.

### Без MCP vs З MCP

```
Без MCP:
  Кожен AI-інструмент × кожне джерело = N×M інтеграцій
  
  Claude ──── GitHub (кастомна інтеграція)
  Claude ──── Slack (кастомна інтеграція)
  Cursor ──── GitHub (ІНША кастомна інтеграція)
  Cursor ──── Slack (ІНША кастомна інтеграція)

З MCP:
  Один стандарт для всіх
  
  Claude ─┐
  Cursor ─┤── MCP ──── GitHub MCP Server
  VS Code ─┘          Slack MCP Server
                       Ваш MCP Server
```

### MCP в цифрах (2026)

- 10,000+ публічних MCP-серверів
- 97M+ місячних завантажень SDK
- Підтримка: Claude, ChatGPT, Cursor, VS Code, Gemini

---

## 11.2 Архітектура MCP

MCP використовує клієнт-серверну модель:

```
┌──────────────────┐     ┌──────────────────┐
│   MCP Client     │     │   MCP Server     │
│  (ваш AI-агент)  │────▶│  (джерело даних) │
│                  │◀────│                  │
│  - Запитує tools │     │  - Tools         │
│  - Читає ресурси│     │  - Resources     │
│  - Використовує │     │  - Prompts       │
│    промпти      │     │                  │
└──────────────────┘     └──────────────────┘
```

### Три типи можливостей MCP-сервера

| Тип | Що це | Приклад |
|-----|-------|---------|
| **Tools** | Дії які агент може виконати | `create_issue`, `send_email`, `query_db` |
| **Resources** | Дані які агент може прочитати | `file://docs/api.md`, `db://users/123` |
| **Prompts** | Шаблони промптів для типових задач | `summarize_pr`, `review_code` |

---

## 11.3 Використання MCP-серверів у своєму коді

### З AI SDK 6

```typescript
import { generateText } from 'ai';
import { openai } from '@ai-sdk/openai';
import { createMCPClient } from '@ai-sdk/mcp';

// Підключаємось до MCP-сервера файлової системи
const fsServer = await createMCPClient({
  transport: {
    type: 'stdio',
    command: 'npx',
    args: ['-y', '@modelcontextprotocol/server-filesystem', '/path/to/project'],
  },
});

// Отримуємо інструменти з MCP-сервера
const tools = await fsServer.tools();

// Використовуємо їх з LLM
const { text } = await generateText({
  model: openai('gpt-4o-mini'),
  tools,
  maxSteps: 5,
  prompt: 'Прочитай файл README.md і поясни що робить цей проект',
});

// Не забудьте закрити з'єднання
await fsServer.close();
```

### Популярні MCP-сервери

| Сервер | Що робить | Пакет |
|--------|----------|-------|
| **Filesystem** | Читання/запис файлів | `@modelcontextprotocol/server-filesystem` |
| **GitHub** | Issues, PRs, repos | `@modelcontextprotocol/server-github` |
| **PostgreSQL** | SQL запити до бази | `@modelcontextprotocol/server-postgres` |
| **Slack** | Повідомлення, канали | `@modelcontextprotocol/server-slack` |
| **Google Drive** | Документи, таблиці | `@anthropic-ai/mcp-server-gdrive` |
| **Brave Search** | Пошук в інтернеті | `@anthropic-ai/mcp-server-brave-search` |

---

## 11.4 Створення власного MCP-сервера

Створимо MCP-сервер для бази знань вашої компанії:

```typescript
// knowledge-base-server.ts
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { z } from 'zod';

const server = new McpServer({
  name: 'company-knowledge-base',
  version: '1.0.0',
});

// Tool: пошук по базі знань
server.tool(
  'search_knowledge',
  'Пошук відповіді в корпоративній базі знань',
  {
    query: z.string().describe('Пошуковий запит'),
    category: z.enum(['product', 'support', 'hr', 'all']).default('all'),
    limit: z.number().min(1).max(10).default(3),
  },
  async ({ query, category, limit }) => {
    const results = await knowledgeBase.search(query, { category, limit });
    return {
      content: [{
        type: 'text',
        text: JSON.stringify(results, null, 2),
      }],
    };
  }
);

// Tool: створення тікету підтримки
server.tool(
  'create_ticket',
  'Створити тікет підтримки для клієнта',
  {
    title: z.string(),
    description: z.string(),
    priority: z.enum(['low', 'medium', 'high', 'critical']),
    customerEmail: z.string().email(),
  },
  async ({ title, description, priority, customerEmail }) => {
    const ticket = await ticketSystem.create({
      title, description, priority, customerEmail,
    });
    return {
      content: [{
        type: 'text',
        text: `Тікет створено: #${ticket.id}`,
      }],
    };
  }
);

// Resource: FAQ документ
server.resource(
  'faq',
  'company://faq',
  'FAQ — часті питання клієнтів',
  async () => ({
    contents: [{
      uri: 'company://faq',
      mimeType: 'text/markdown',
      text: await readFile('./data/faq.md', 'utf-8'),
    }],
  })
);

// Запуск сервера
const transport = new StdioServerTransport();
await server.connect(transport);
```

```bash
# Запуск
npx tsx knowledge-base-server.ts
```

---

## 11.5 Безпека MCP

MCP — це потужний інструмент, але він несе серйозні ризики безпеки:

### Загрози

| Загроза | Опис | Захист |
|---------|------|--------|
| **Supply chain** | Шкідливий MCP-сервер з npm | Перевіряйте пакети, pin versions |
| **Tool poisoning** | Сервер змінює опис tool щоб маніпулювати LLM | Аудит tool descriptions |
| **Exposed servers** | MCP-сервер без автентифікації | Завжди використовуйте OAuth 2.1 |
| **Remote code execution** | Вразливості в mcp-remote | Оновлюйте залежності |

### Чекліст безпеки

```typescript
// 1. Pin версії MCP-пакетів
// package.json — НЕ використовуйте "latest"
{
  "@modelcontextprotocol/server-github": "1.2.3"  // Точна версія
}

// 2. Обмежуйте доступ — принцип мінімальних привілеїв
const fsServer = await createMCPClient({
  transport: {
    type: 'stdio',
    command: 'npx',
    args: [
      '@modelcontextprotocol/server-filesystem',
      '/app/data',   // Тільки конкретна директорія
      '--readonly',  // Тільки читання
    ],
  },
});

// 3. Валідуйте результати від MCP-серверів
// Ніколи не довіряйте даним від MCP-сервера на 100%
```

---

## Перевір себе

1. Що таке MCP і чим він відрізняється від звичайного REST API?
2. Назвіть 3 типи можливостей MCP-сервера з прикладами
3. Як підключити MCP-сервер GitHub до свого AI-агента?
4. Створіть простий MCP-сервер з одним tool та одним resource
5. Які загрози безпеки несе MCP і як від них захищатись?

---

**Назад:** [← Модуль 10 — RAG](10-rag.md) | **Далі:** [Модуль 12 — Agent Frameworks →](12-agent-frameworks.md)
